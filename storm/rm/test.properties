rm.test_directory = rm_files
rm.max_waiting_time_in_msec=60000
rm.test_number_of_files=10

## grinder properties 

# relative paths are evaluated from the directory containing the
# properties file. The default is "grinder.py".
grinder.script = rm_files.py