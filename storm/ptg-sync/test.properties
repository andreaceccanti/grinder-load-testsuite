ptg-sync.test_directory = PtG
ptg-sync.sleep_threshold=50
ptg-sync.sleep_time=.5
ptg-sync.do_handshake=False

# Relative paths are evaluated from the directory containing the
# properties file. The default is "grinder.py".
grinder.script = ptg-sync.py
